skill: /analyse
version: v5
input:
  required:
    - name: brief
      type: string
  optional:
    - name: carte_de_sens
      type: string
      description: "Carte de sens structuree (si /oriente a ete execute en amont)"
output:
  format: markdown
  sections:
    - livrable_principal
    - log_orchestration
    - limites
    - handoff (si piping)
anti_sycophancy_profile: critique
